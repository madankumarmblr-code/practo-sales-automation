# Cloudflare Pages — static UI only (see HOSTING.md).
# Full stack (Express + SQLite) cannot run on Workers; use Docker/VPS for API.
name = "practo-sales-automation"
compatibility_date = "2025-08-01"
pages_build_output_dir = "frontend/dist"
